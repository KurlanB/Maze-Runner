package ca.mcmaster.se2aa4.mazerunner.Runner;

import ca.mcmaster.se2aa4.mazerunner.Map.Maze;

/**
 * Sample class for Factory Method Pattern, may be used in future.
 * 
 * The BFS class implements the MazeRunner interface and provides
 * an algorithm to escape the maze using the Breadth-First Search (BFS) algorithm.
 */
public class BFS implements MazeRunner {

    /**
     * Escapes the maze using the BFS algorithm.
     * 
     * @param maze The maze to escape from.
     * @return A string representing the path taken to escape the maze.
     */
    @Override
    public String escapeMaze(Maze maze) {
        // Implement BFS algorithm to escape the maze
        return "Not yet implemented";
    }
    
}
